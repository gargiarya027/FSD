# ABES Department Achievement Tracker (minimal)

Features:
- Add achievements (no default items included)
- Search by title, student name, faculty name, year
- Filter by category, department, and date range
- Delete achievements
- Counts per department shown
- Minimal black & white UI
- Data stored in browser localStorage

To run:
1. npm install
2. npm run dev
